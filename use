#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# shellcheck source=scripts/sync-lib.sh
source "$SCRIPT_DIR/scripts/sync-lib.sh"

run_use_main "$@"
